import {View, Text} from 'react-native';
import React from 'react';

const HistoryScreen = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text style={{fontSize: 30, fontWeight: '700'}}>On Progress ...</Text>
    </View>
  );
};

export default HistoryScreen;
